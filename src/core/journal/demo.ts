/**
 * Тетрадь, которая лежит на полке с самого начала.
 *
 * Пустая и в линейку — заведённая, но не исписанная. Наполнять её образцовым
 * конспектом было бы враньём того же рода, что и нарисованный в фотошопе скрин
 * в презентации: смотреть надо на то, что рисуется здесь и сейчас пером.
 *
 * Объект один на страницу: и библиотека (корешок на полке), и стор тетради
 * должны получить одну и ту же тетрадь, а не две одинаковых.
 */
import { newJournal } from './journal';
import type { JournalDoc } from './types';

let cached: JournalDoc | null = null;

export function demoJournal(): JournalDoc {
  cached ??= newJournal('Field Notes', 8, 'ruled');
  return cached;
}
